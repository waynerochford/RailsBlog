<%= form_for([@blog, @post]) do |f| %>
 <% if @post.errors.any? %>
 <div id="error_explanation">
 <h2><%= pluralize(@post.errors.count, "error") %> prohibited this blog from being saved:</h2>
 <ul>
 <% @post.errors.full_messages.each do |msg| %>
 <li><%= msg %></li>
 <% end %>
 </ul>
 </div>
 <% end %>
 <div class="field">
 <p>
 <%= f.label :post_message %><br />
 <%= f.text_area :post_message %>
 </p>
 </div>
 <div class="actions">
 <%= f.submit %>
 </div>
<% end %>
